;;; apparmor.el --- Apparmor mode -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(use-package apparmor-mode
  :defer t
  :mode ("apparmor" . apparmor-mode))


(provide 'modes/apparmor)
;;; apparmor.el ends here
